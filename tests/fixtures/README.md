# Tests Fixtures & Golden Dataset

This directory contains the files used for regression testing across refactoring phases:

- `fixtures/`: Document inputs categorized by structural type.
- `expected/`: Expected baseline parser outputs, document graphs, chunk results, and retrieval matches.
